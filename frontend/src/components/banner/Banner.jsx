import React from 'react'
import classes from "./Banner.module.css"
const Banner = () => {
  return (
    
    <section className={classes.banner}>
    <div className={classes.dark}>
    <h1 className={classes.heading}>welcome to food App</h1>
      </div>

    </section>
  )
}

export default Banner
